import sys

def solve(x_s, y_s, x_t, y_t, x_p, y_p):
    return 0

if __name__ == '__main__':
    input = sys.stdin.read()
    x_s, y_s, x_t, y_t, x_p, y_p = map(int, input.split())
    print(solve(x_s, y_s, x_t, y_t, x_p, y_p))
# Generator time: 28.0451 seconds