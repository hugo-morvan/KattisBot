# Read the input string
input_string = input()

# Count the number of 'u' characters in the input string
uncertainty_level = input_string.count('u')

# Print the uncertainty level
print(uncertainty_level)